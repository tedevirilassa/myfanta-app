{
  "name": "myfanta-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "prisma generate && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install --with-deps chromium",
    "postinstall": "prisma generate && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed.js",
    "render:migrate": "node scripts/sync-to-render.js --migrate-only",
    "render:sync": "node scripts/sync-to-render.js",
    "render:migrate-and-sync": "node scripts/sync-to-render.js --migrate",
    "render:pull": "node scripts/sync-from-render.js",
    "render:pull-dry": "node scripts/sync-from-render.js --dry-run",
    "render:pull-fantapresidenti": "node scripts/pull-fantapresidenti.js",
    "render:pull-fantapresidenti-dry": "node scripts/pull-fantapresidenti.js --dry-run",
    "prod:migrate": "node scripts/sync-to-prod.js --migrate-only",
    "prod:sync": "node scripts/sync-to-prod.js",
    "prod:migrate-and-sync": "node scripts/sync-to-prod.js --migrate",
    "db:backup": "node scripts/backup-db.js",
    "db:backup-remote": "node scripts/backup-db.js --remote",
    "db:restore": "node scripts/restore-db.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "ejs": "^5.0.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "nodemailer": "^8.0.10",
    "pg": "^8.20.0",
    "playwright": "^1.59.1",
    "playwright-extra": "^4.3.6",
    "prisma": "^7.8.0",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
