npm run prod:deploy-install
sync codice DEV → fantasserver (SSH+SFTP) + npm ci + prisma generate

npm run prod:deploy
sync solo codice (senza npm ci)

npm run prod:deploy-dry
mostra cosa verrebbe copiato senza scrivere

npm run prod:migrate-and-sync
applica migration + sync dati DB locale → fantasserver (via SSH tunnel)

npm run prod:sync
sync solo dati DB (senza migration)

npm run prod:migrate
solo migration, niente sync dati

npm run dev
avvia il sito in locale

npm run db:backup
backup DB locale

npm run db:backup-remote
backup del DB su fantasserver